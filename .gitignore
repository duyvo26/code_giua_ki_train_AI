# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Dữ liệu & kết quả (tải/tạo tự động khi chạy)
data/*.csv
data/processed/
models/best_model/
results/

# Bí mật - token bot không bao giờ commit
utils/bot_config.json

# Bí mật - API key extension không bao giờ commit
utils/api_keys.json

# Bí mật - lịch hẹn quét tự động không bao giờ commit
utils/schedules.json

# Môi trường
.venv/
venv/
.env

# macOS
.DS_Store
